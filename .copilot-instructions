See `docs\design\style-guide.md` for project style rules.
Do not write code for backward compatibility purposes.
The WPF application must never synchronously await results from the entity repository.
NuGet API research artifacts are maintained under `docs\nuget\research\` and generated by `.\scripts\research-nuget-apis.ps1`.
If NuGet API research is needed, regenerate with `.\scripts\research-nuget-apis.ps1` instead of manual edits.
