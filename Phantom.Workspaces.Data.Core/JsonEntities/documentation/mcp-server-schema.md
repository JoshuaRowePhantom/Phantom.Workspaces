# MCP Server Schema

Schema for Model Context Protocol (MCP) server registrations. Describes how to connect to an MCP server (remote http(s) endpoint with an optional API key, or a local `stdio://?command=...` endpoint), along with its approval policy and optional allow-list of tools.
