# Workspace Entity Tool Instructions

Before using workspace entity tools, retrieve the global note named `["documentation", "entity-workspace-agent-tool-instruction-details"]` and follow its instructions exactly.
