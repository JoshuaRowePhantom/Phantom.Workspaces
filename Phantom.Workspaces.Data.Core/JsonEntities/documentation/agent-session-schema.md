# Agent Session Schema

Schema for workspace entities with entity type `agent-session`.

The `agent-session-id` field stores the agent session identifier used by `IAgentPersistenceStore` requests and persisted agent records.
