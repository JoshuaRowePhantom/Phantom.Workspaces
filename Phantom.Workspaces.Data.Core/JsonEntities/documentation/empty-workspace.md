# Empty Workspace

This workspace has no regions or tabs configured yet.

Add content by editing the workspace entity and defining one or more entries in `regions`, each with a `tabs` collection.

Use `documentation/workspace-schema` as the reference for the workspace shape, then point tab content to target entities (for example, documentation notes, views, or other entities).
