{
  "$id": "https://schemas.workspaces.phantom.to/workspaces/data/core/task.json",
  "description": "Schema for task entities that capture actionable work items and task metadata.",
  "allOf": [
    {
      "$ref": "entity.json"
    }
  ],
  "type": "object",
  "required": [
    "entity-types"
  ],
  "properties": {
    "entity-types": {
      "type": "array",
      "items": {
        "$ref": "core.json#/$defs/entity-type-id"
      },
      "contains": {
        "const": "task"
      }
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "in-progress",
        "completed",
        "blocked",
        "cancelled"
      ],
      "description": "Current status of the task"
    },
    "assigned-to": {
      "type": "string",
      "description": "Source-system-dependent assignee value carried over from the originating system (for example, an Azure DevOps assignee). Its exact meaning and format vary by source system. This raw value is not an entity reference; the entity classifier uses it to choose which user to apply the 'assigned-to' interest to."
    }
  }
}
